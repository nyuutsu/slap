# VCDIFF CoreOnly apply cross-validation
# Each row: base ROM | patch | label
#
# slap applies the patch and the output is compared, byte for byte,
# against xdelta3 applying the same patch — never slap against slap.
# Gated on xdelta3 and on the fixtures: base ROMs are local-only as
# ever, and the constructors-wc patch is local-only too (its author's
# readme forbids redistribution; test/data's README has the path to
# place it at). Rows skip cleanly where anything is absent.
#
# All three patches are confirmed CoreOnly: header indicator none,
# every window VCD_SOURCE-only (no VCD_TARGET, no per-window Adler32),
# no secondary compression. The "AF" Karateka pair is multi-window;
# constructors-wc is a single RUN-heavy window.

test/data/karateka/base.nes | test/data/karateka/indonesia-tl/patch.xdelta | karateka-tl
test/data/karateka/base.nes | test/data/karateka/indonesia-hack/patch.xdelta | karateka-hack
test/data/wrecking-crew/base.fds | test/data/wrecking-crew/constructors/patch.xdelta | constructors-wc
