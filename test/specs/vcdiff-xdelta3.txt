# VCDIFF xdelta3 apply cross-validation
# Each row: base ROM | patch | label
#
# Patches here are unmodified xdelta3 output — application header,
# per-window Adler32, and (where the producer chose it) LZMA secondary
# compression intact. slap applies the patch and the output is
# compared, byte for byte, against xdelta3 applying the same patch —
# never slap against slap. Gated on xdelta3 and on the base ROM, which
# is local-only, so rows skip cleanly where either is absent.
#
# pokesta2-debug-lzma is the first compressed row, keeping this
# header's old promise: a wild LZMA patch (8 windows, 64 MB output)
# against the stadium2 fixture base.
#
# pokesta2-debug-fgk and pokesta2-debug-djw are converted provenance:
# the same debug-menu change re-encoded by the reference tool (xdelta3 -S fgk / -S djw) —
# the two compressors no wild patch in the collection carries.

test/data/stadium2/base.z64 | test/data/stadium2/debug-menu/patch.xdelta | pokesta2-debug-lzma
test/data/stadium2/base.z64 | test/data/stadium2/debug-menu-fgk/patch.xdelta | pokesta2-debug-fgk
test/data/stadium2/base.z64 | test/data/stadium2/debug-menu-djw/patch.xdelta | pokesta2-debug-djw
