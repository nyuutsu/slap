packages: .

-- tasty-stats and encoding cap dependencies below the versions we build
-- against; both build fine past those caps, so relax them here.
allow-newer:
    tasty-stats:*
  , encoding:containers
